= simple_form_for(@club_admin_role) do |f|
  = f.error_notification

  div class="form-inputs"
    = f.association :club
    = f.association :user
    = f.input :name

  div class="form-actions"
    = f.button :submit
