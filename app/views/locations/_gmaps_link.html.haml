- if location
  = link_to "http://maps.google.com/?q=#{location.address.squish}", {rel: "nofollow", target: '_blank', class: 'hover:underline'} do
    %span{title: location.address}= location.name
- else
  %em Lieu non défini
