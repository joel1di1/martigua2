= simple_form_for [current_section, @team] do |f|
  = f.error_notification

  div class="form-inputs"
    = f.association :club
    = f.association :sections, selected: current_section&.id
    = f.input :name

  div class="form-actions"
    = f.button :submit
