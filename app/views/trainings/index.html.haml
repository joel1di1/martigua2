%h2 Entrainements

.table-responsive
  %table.table.table-hover.table-striped
    %thead
      %tr
        %th
          Date
        %th
          Horaire
        %th
          Lieu
        %th
          Présences
        %th
          Ma dispo
        %th
          Actions
    %tbody
      = render @trainings

= link_to 'Ajouter un entrainement', new_section_training_path(section_id: current_section)