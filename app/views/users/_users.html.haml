.table-responsive
  %table.table.table-hover.table-striped
    %thead
      %tr
        %th Nom
        %th Email
        %th Tel
        - if current_section
          %th Roles

    %tbody
      - group = nil unless defined?(group)
      = render partial: 'users/user', collection: users, locals: { group: group }

