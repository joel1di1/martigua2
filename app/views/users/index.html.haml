.table-responsive
  %table.table.table-hover.table-striped
    %thead
      %tr
        %th Prénom
        %th Nom
        %th Surnom
        %th Email
        %th Tel
    %tbody
      = render @users

- if current_section && current_user.is_coach_of?(current_section)
  = link_to 'Ajouter un joueur', new_section_section_user_invitation_path(current_section)
  