# frozen_string_literal: true

describe 'manage club admins', :devise do
  it 'admin adds a new admin' do
    # club = create :club
    # section = create :section, club: club
    # admin = create :user, with_club_as_admin: admin
    # player = create :user, with_section: section

    # signin admin.email, admin.password
  end
end
